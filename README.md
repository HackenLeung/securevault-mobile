# SecureVault

SecureVault is a local-first password manager prototype built with Expo Router and React Native.

## Run

```bash
npm install
npm run start
```

The current implementation focuses on matching the provided UI mockups with navigable screens and sample data.
